export const SHAPE_TEMPLATES = {
  tabular: (w, h) =>
    `M0,${h * 0.15}
     L${w * 0.05},0
     L${w * 0.95},0
     L${w},${h * 0.15}
     L${w},${h}
     L0,${h} Z`,

  blocky: (w, h) =>
    `M0,${h * 0.1}
     L${w},0
     L${w},${h * 0.85}
     L0,${h} Z`,

  domed: (w, h) =>
    `M0,${h * 0.3}
     Q${w * 0.5},0 ${w},${h * 0.3}
     L${w},${h}
     L0,${h} Z`,

  wedge: (w, h) =>
    `M0,${h * 0.35}
     L${w},0
     L${w},${h}
     L0,${h} Z`,

  pinnacle: (w, h) =>
    `M${w * 0.5},0
     L${w * 0.65},${h * 0.25}
     L${w * 0.85},${h * 0.1}
     L${w},${h * 0.3}
     L${w},${h}
     L0,${h}
     L0,${h * 0.3}
     L${w * 0.15},${h * 0.15}
     L${w * 0.35},${h * 0.2}
     Z`,

  drydock: (w, h) =>
    `M0,${h * 0.2}
     L${w * 0.35},0
     L${w * 0.35},${h * 0.25}
     L${w * 0.65},${h * 0.25}
     L${w * 0.65},0
     L${w},${h * 0.2}
     L${w},${h}
     L0,${h} Z`,
};
